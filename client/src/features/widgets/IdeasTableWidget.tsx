import { ListChecks } from 'lucide-react'
import { WidgetStub } from './WidgetStub'

export function IdeasTableWidget() {
  return (
    <WidgetStub icon={ListChecks}>
      Teknik alım-satım önerileri tablosu — durum etiketli.
    </WidgetStub>
  )
}
