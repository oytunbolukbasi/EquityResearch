import { Newspaper } from 'lucide-react'
import { WidgetStub } from './WidgetStub'

export function MorningNoteWidget() {
  return <WidgetStub icon={Newspaper}>Günlük morning note — Top Call + makro maddeler.</WidgetStub>
}
