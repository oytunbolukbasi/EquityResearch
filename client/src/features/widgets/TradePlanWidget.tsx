import { LineChart } from 'lucide-react'
import { WidgetStub } from './WidgetStub'

export function TradePlanWidget() {
  return (
    <WidgetStub icon={LineChart}>Trade plan viewer — giriş bandı, TP1/TP2/TP3, hard SL.</WidgetStub>
  )
}
